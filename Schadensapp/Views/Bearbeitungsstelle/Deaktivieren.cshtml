@model Schadensapp.Models.BearbeitungsstellenView

@{
    ViewData["Title"] = "Deaktivieren";
}

<h1>Deaktivieren</h1>

<h3>Sind Sie sicher, dass Sie diese Bearbeitungsstelle deaktivieren möchten?</h3>
<h4>Nach der Deaktivierung kann diese Bearbeitungsstelle bei der Erstellung einer Meldung nicht mehr ausgewählt werden.</h4>
<div>
    <h4>Bearbeitungsstelle</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            MAP-Gruppe
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Bearbeitungsstelle.MapGruppe)
        </dd>
        <dt class="col-sm-2">
            Name
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Bearbeitungsstelle.Name)
        </dd>
        <dt class="col-sm-2">
            Telefonnummer
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Bearbeitungsstelle.Telefone)
        </dd>
        <dt class="col-sm-2">
            E-Mail
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Bearbeitungsstelle.EMail)
        </dd>
    </dl>
    <br />
    <br />
    
    @if (Model.LiegenschaftenListe != null && Model.LiegenschaftenListe.Count >= 1)
    {
        <h4>Achtung, bei einer Deaktivierung werden folgende Liegenschaften mit deaktiviert. Um eine Deaktivierung der Liegenschaften zu verhindern müssen diese vorher einer anderen Bearbeitungsstelle zugewiesen werden.</h4>
        <br />
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Name
                    <th>
                        Straße
                    </th>
                    <th>
                        Hausnummer
                    </th>
                    <th>
                        Postleitzahl
                    </th>
                    <th>
                        Stadt
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.LiegenschaftenListe)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Strasse)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Hausnummer)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Postleitzahl)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Stadt)
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    }
    <br />
    <form asp-action="Deaktivieren">
        <input type="hidden" asp-for="Bearbeitungsstelle.BearbeitungsstelleID" />
        <input type="submit" value="Deaktivieren" class="btn btn-danger" /> |
        <a asp-action="Index">Zurück zur Liste</a>
    </form>
</div>
